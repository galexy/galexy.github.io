---
createdAt: 2025-12-25
tags: ["git", "productivity"]
---

`git worktree` lets you check out multiple branches simultaneously in different directories!

```bash
git worktree add ../feature-branch feature-branch
```

This creates a new directory with the branch checked out. Super useful when you need to quickly switch contexts or compare branches without stashing changes.
